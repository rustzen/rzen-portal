---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'RustZen',
  description = 'macOS tools for developer cleanup and clipboard history.',
} = Astro.props;

const navItems = [
  { href: '/zen-clear', label: 'Zen Clear' },
  { href: '/clipboard', label: 'Clipboard' },
  { href: '/blog', label: 'Blog' },
];

const currentPath = Astro.url.pathname.replace(/\/$/, '') || '/';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>{title}</title>
  </head>
  <body>
    <header class="site-header">
      <a href="/" class="brand" aria-label="RustZen home">
        <img class="brand-mark" src="/rustzen-mark.svg" alt="" width="32" height="32" />
        <span class="brand-word">RustZen</span>
      </a>
      <nav class="site-nav" aria-label="Primary navigation">
        {
          navItems.map((item) => (
            <a
              href={item.href}
              class:list={{
                active:
                  currentPath === item.href || currentPath.startsWith(`${item.href}/`),
              }}
            >
              {item.label}
            </a>
          ))
        }
      </nav>
    </header>
    <slot />
    <footer class="site-footer">
      <p>RustZen builds Zen Clear and Clipboard for local macOS work.</p>
      <a href="/blog">Read the blog</a>
    </footer>
  </body>
</html>
